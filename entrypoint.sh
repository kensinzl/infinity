#! /bin/bash
# su - dev -c "cd /build"
cd /build
# mvn package

# cd /build/target/
#su - dev -c "cd /build/target/"

mvn spring-boot:run

#chmod 777 /build/target/infinity-1.0.0-SNAPSHOT.jar

#java -jar infinity-1.0.0-SNAPSHOT.jar




# 1. 
# shell file is very simple, just put the command in.
# just enter the source code folder and then run the mvn spring-boot:run is enough.		